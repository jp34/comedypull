import "../css/ShowGallery.css";

const ShowGallery = ({ children }) => {
    return (
        <div className="show-gallery">
            {children}
        </div>
    );
}

export default ShowGallery;